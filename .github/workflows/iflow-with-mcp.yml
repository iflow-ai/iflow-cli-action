name: iFlow with MCP

on:
  # workflow_run:
  #   workflows: ["Build and Deploy Homepage"]
  #   types:
  #     - completed
  # Allow manual trigger
  workflow_dispatch:
  push:
    # Ignore tag pushes so creating or pushing tags won't trigger this workflow
    tags-ignore: ['*']
    paths: ['**/*.rs', 'Cargo.toml', 'Cargo.lock', 'tests/**', 'examples/**']
  pull_request:
    paths: ['**/*.rs', 'Cargo.toml', 'Cargo.lock', 'tests/**', 'examples/**']


permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAGES: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: iFlow CLI with MCP
        uses: ./
        with:
          prompt: "use deepwiki mcp server to search how to use Skynet to build a game"
          api_key: ${{ secrets.IFLOW_API_KEY }}
          settings_json: |
            {
                "selectedAuthType": "iflow",
                "apiKey": "${{ secrets.IFLOW_API_KEY }}",
                "baseUrl": "https://apis.iflow.cn/v1",
                "modelName": "qwen3-coder-plus",
                "searchApiKey": "${{ secrets.IFLOW_API_KEY }}",
                "mcpServers": {
                    "deepwiki": {
                        "command": "npx",
                        "args": ["-y", "mcp-deepwiki@latest"]
                    }
                }
            }
          model: "qwen3-coder-plus"
          timeout: "3600"
          debug: "true"
          gh_version: "2.76.2"
